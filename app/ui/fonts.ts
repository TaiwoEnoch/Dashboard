import { Inter } from 'next/font/google';
import { Lusitana } from 'next/font/google';

// Specify weights for each font
export const inter = Inter({ 
  subsets: ['latin'], 
  weight: ['400', '700'] // Add the weight property
});

export const lusitana = Lusitana({ 
  subsets: ['latin'], 
  weight: ['400', '700'] // Add the weight property
});
